Funcionamiento endpoint para filtrar animales:

- Filtrar species múltiples:
/api/animals?species=perro,gato

- Filtrar breeds múltiples:
/api/animals?breed=siames,boxer,buldog

- Filtro mixto + rango edad:
/api/animals?species=gato&age_min=2&age_max=6

- Ordenar por edad (descendente):
/api/animals?orderBy=age&direction=desc

- Paginación:
/api/animals?page=2&limit=12

- Todo junto:
/api/animals?
species=gato,perro&
breed=siames,pastor&
age_min=1&
age_max=8&
orderBy=age&
direction=asc&
page=1&
limit=20


NOTAS:

Genial. Bueno, ya tengo todo funcionando a excepcion de una cosa. En la edicion del perfil (admin o user), cuando se modifica el nombre o el email, se aplican los cambios correctamente, pero de repente se muestra una pagina con nombre undefined y sin datos de usuario. Es decir, como si un usuario estuviera logueado pero sin sus datos.
Entonces, dime si es posible implementar que, cuando se actualice nombre o email en el componente profile-edit, se haga llamada a backend de nuevo para recargar correctamente los datos. Es sencillo aplicar esto? Piensa que esto debe ocurrir justo después de que el popup de confirm-password-dialog haga su trabajo.

IMPORTANTE:
22. Arreglar redireccion cuando se modifica el nombre de un perfil.
10. Modificar el tamaño de fuente en los botones y todo.
Revisar texto se solicitudes en usuarios.

